# Django
SECRET_KEY=dev-secret-key-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
DB_NAME=music_studio
DB_USER=studio_user
DB_PASSWORD=studio_password
DB_HOST=localhost
DB_PORT=5432

# Redis
REDIS_URL=redis://localhost:6379/0

# MinIO
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minio_admin
MINIO_SECRET_KEY=minio_password
MINIO_BUCKET=music-studio

# Email (SendGrid) - Add your keys
SENDGRID_API_KEY=
DEFAULT_FROM_EMAIL=noreply@musicstudio.local

# SMS (Twilio) - Add your credentials
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# Frontend
# If developing remotely, change localhost to your remote server's IP address
# unless you are using SSH port forwarding.
NEXT_PUBLIC_API_URL=http://localhost:8000/api
